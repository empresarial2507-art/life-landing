# LIFE — Landing (v5)

Site estático (HTML único) da landing do LIFE. Hero com mockup do app.

- `index.html` — página
- `img/hero-app.webp` / `.png` — imagem do hero (mockup iPhone)

Deploy: qualquer host estático (Vercel/Netlify/GitHub Pages). Sem build.

Nota: demais imagens (transformações, seções) ainda são hotlinkadas do WordPress da Paloma; a página está com `noindex` (era ambiente de teste).
